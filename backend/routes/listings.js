/**
 * Listings routes - mount on app in server.js if needed.
 * Currently listing endpoints are defined in server.js.
 */
const express = require('express');
const router = express.Router();

module.exports = router;
